apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opsboard
  namespace: opsboard
spec:
  rules:
    - host: opsboard-staging.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opsboard-frontend
                port:
                  number: 80
